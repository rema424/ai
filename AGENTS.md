# Repository Instructions

## Skill Improvement Scope

When a task in this repository asks for skill improvement work, the scope is limited to repository assets such as `skills/` and related repository metadata.

Installed local skills outside this repository, including `/Users/rm/.codex/skills/`, are out of scope unless the user explicitly asks to modify them. Do not mirror repository skill changes into installed local skills by default.
